# Local fixtures

Model weights, indexes, audio, NumPy arrays, and generated waveforms are ignored
by Git. Place local reference bundles here using the layout described in
`docs/REFERENCE_TESTING.md`.

Do not redistribute a voice model unless its license permits redistribution.

